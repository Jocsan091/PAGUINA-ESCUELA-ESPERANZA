---
import {
  formatNewsDate,
  getNewsDate,
  getNewsSummary,
  getNewsImage,
  listarUltimasNoticias,
} from "../../lib/noticias";
import { rutas } from "../../lib/rutas";

const ultimasNoticias = await listarUltimasNoticias(3);
---

<section class="home-news-section">
  <div class="home-news-header">
    <span class="home-news-kicker">Ultimas noticias</span>
    <h2>Informacion reciente de la Escuela Esperanza</h2>
  </div>

  {
    ultimasNoticias.length > 0 ? (
      <>
        <div class="home-news-grid">
          {ultimasNoticias.map((noticia) => (
            <article class="news-card">
              <div class="news-card-image-wrap">
                <img src={getNewsImage(noticia.data.image)} alt={noticia.data.title} />

                <div class="news-date-badge">
                  <span class="news-date-icon">
                    <i class="fa-solid fa-book-open"></i>
                  </span>
                  <span class="news-date-text">{formatNewsDate(getNewsDate(noticia))}</span>
                </div>
              </div>

              <div class="news-card-content">
                <h3 class="news-card-title">{noticia.data.title}</h3>
                <p>{getNewsSummary(noticia)}</p>
              </div>
            </article>
          ))}
        </div>

        <div class="home-news-actions">
          <a href={rutas.noticias} class="home-news-button">Ver mas</a>
        </div>
      </>
    ) : (
      <div class="news-empty-state">
        <p>No hay noticias publicadas.</p>
        <a href={rutas.noticias} class="home-news-button">Ir a noticias</a>
      </div>
    )
  }
</section>
